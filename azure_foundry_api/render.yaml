services:
  - type: web
    name: seller-intelligence-azure-foundry-api
    env: python
    buildCommand: pip install -r backend/requirements.txt
    startCommand: PYTHONPATH=backend uvicorn azure_foundry_api.app.main:app --host 0.0.0.0 --port $PORT --app-dir .
